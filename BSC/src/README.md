# Bluespec Classic Source

Bluespec Classic (`.bs`) source for the preserved qrisc32 architectural model.
The implementation gate now lives in `../rtl/`; this model remains a regression
oracle under the `model-*` Makefile targets.

Keep generated compiler and simulator output under `../build/`.
